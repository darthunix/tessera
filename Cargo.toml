[workspace]
members = ["crates/tessera-core", "crates/tessera-capi", "crates/tessera-kernels"]
resolver = "3"

[workspace.package]
version = "0.1.0"
edition = "2024"
rust-version = "1.98.1"
license = "PostgreSQL"
publish = false

[workspace.dependencies]
anyhow = "1.0.104"
tessera-core = { path = "crates/tessera-core" }
tessera-kernels = { path = "crates/tessera-kernels" }

[workspace.lints.rust]
unsafe_op_in_unsafe_fn = "deny"

[profile.dev]
panic = "unwind"

[profile.release]
panic = "unwind"
